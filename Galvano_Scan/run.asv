



closeup();
%% 
clear s;
while true
    move_to_angle('COM3', 115200, 50, 0, 163);
    
    move_to_angle('COM3', 115200, 50, 50, 163);
    move_to_angle('COM3', 115200, 0, 50, 163);
    move_to_angle('COM3', 115200, 0, 0, 163);
    % pause(0.5);
end

%% 
s = serialport("COM3", 115200);
dataToSend = 
while true
    write(s, dataToSend, "uint8");
    pause(0.2);
end
